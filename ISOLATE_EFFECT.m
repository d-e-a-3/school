% plot(y1,'b--')
% hold on
plot(diff(y2))
    xlim([20 100]);
% hold off
% calculate differences between adjacent data points.
% define threshold value for locating initial temp rise.
% identify the array location for superthreshold diff. 
% truncate array elements before that element and again at t=30s
% --OR--
% use options in 'fit' function to exclude data that is:
% a. in the very begining of the data set, and
% b. does not vary more than the threshold amount from its nearest
% neighbor.
% 
% 
exclusionThreshold = 0.03
exclude =...
    for
        if diff(
        elseif 
        else y = 0
        end
    end

            
    